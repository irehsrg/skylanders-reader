# Public Supabase values baked into the production (hosted) build.
# The publishable/anon key is DESIGNED to ship in browser code — data is
# protected by row-level security, not by hiding this key. The service_role
# secret is never here (it lives only in local .env for image uploads).
VITE_SUPABASE_URL=https://opqnhckurkrawlunocfw.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_yWRqRCTZukBVpYXJbBQ1uA_W_3xF8un
